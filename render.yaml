services:
  - type: web
    name: telegram-userbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn userbot:app
    envVars:
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
